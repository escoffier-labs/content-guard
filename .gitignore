__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
dist/
build/
*.egg-info/
.content-guard/
implementation-notes.md
# Untracked private policy files (real identifiers). The *.example.json
# template is intentionally NOT matched by this rule.
*.local.json

# Local operator-report artifacts
.brigade/

# Local brigade memory (cards are local-only, never committed)
/memory/
